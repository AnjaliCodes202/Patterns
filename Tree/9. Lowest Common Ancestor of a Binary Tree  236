// /**
//  * Definition for a binary tree node.
//  * struct TreeNode {
//  *     int val;
//  *     TreeNode *left;
//  *     TreeNode *right;
//  *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
//  * };
//  */
// class Solution {
// public:
//     TreeNode* ans;
//     bool lca(TreeNode* root,TreeNode* p, TreeNode* q){
//         if(!root) return false;
//         if(ans!=NULL) return true;
//         // if(root->val==p->val || root->val==q->val) return true;
//         bool flag1 = lca(root->left,p,q);
//         if(ans) return true;
//         bool flag2 = lca(root->right,p,q);
//         if((root->val==p->val || root->val==q->val)&& (flag1 || flag2) || flag1 && flag2) {
//             if(ans==NULL) ans = root;
//             return true;
//         }else if(flag1 || flag2 || root->val==p->val || root->val==q->val) return true;
//         else return false;
//     }

//     TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
//         ans=NULL;
//         if(root->val==p->val || root->val==q->val) return root;
//         lca(root,p,q);
//         return ans;
//     }
// };

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    
    TreeNode* lca(TreeNode* root, TreeNode* p,TreeNode* q){
        if(!root) return nullptr;
        if(root==p || root==q) return root;
        TreeNode* left = lca(root->left,p,q);
        TreeNode* right = lca(root->right,p,q);
        if(left && right)  return root;
        return left ? left:right;
    }
    

    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
        return lca(root,p,q);
    }
};
